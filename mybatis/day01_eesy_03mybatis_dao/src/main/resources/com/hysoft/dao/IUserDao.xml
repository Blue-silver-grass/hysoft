<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hysoft.dao.IUserDao">
    <!--
    配置查询所有  有了它就有了执行的SQL语句，就可以获取PreparedStatement
    此配置中还有封装的实体类全限定类名
    -->
    <select id="findAll" resultType="com.hysoft.domain.User">
        select * from user;
    </select>
</mapper>